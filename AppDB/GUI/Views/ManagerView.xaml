<UserControl x:Class="GUI.Views.ManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.Views"
             xmlns:parts ="clr-namespace:GUI.Views.MainMenu"
             mc:Ignorable="d" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="5.5*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <parts:OptionsView></parts:OptionsView>
        <ScrollViewer HorizontalScrollBarVisibility="Visible" Grid.Column="1">
            <parts:SelectedOptionView></parts:SelectedOptionView>
        </ScrollViewer>
    </Grid>
</UserControl>
