<UserControl x:Class="GUI.Views.RepresentationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.Views"
             xmlns:users ="clr-namespace:GUI.Views"
             xmlns:parts ="clr-namespace:GUI.Views.MainMenu"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="6*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <parts:HeaderView Grid.ColumnSpan="2"></parts:HeaderView>
        <GridSplitter Grid.Column="0" VerticalAlignment="Bottom"
                      HorizontalAlignment="Stretch" Grid.ColumnSpan="2" 
                      Height="3" Background="Black"></GridSplitter>
        
        <parts:OptionsView Grid.Row="1"></parts:OptionsView>
        <ScrollViewer HorizontalScrollBarVisibility="Visible"  Grid.Row="1" Grid.Column="1">
            <parts:SelectedOptionView></parts:SelectedOptionView>
        </ScrollViewer>
    </Grid>
</UserControl>
